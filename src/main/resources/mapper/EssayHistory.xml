<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="dao.EssayHistoryDao">
    <select id="getEssayHistorys" resultType="domain.EssayHistory">
        select eh.* from essayhistory as eh,essay as e where eh.essayid=e.id
        <if test="search != null and search != ''">
            and e.title like CONCAT('%', #{search}, '%')
        </if>
        order by eh.createtime desc limit #{start},#{size}
    </select>
    <insert id="addEssayHistory">
        insert into essayhistory (essayid,flag,checkmsg) values(#{essayhistory.essayId},#{essayhistory.flag},#{essayhistory.checkmsg})
    </insert>
    <select id="getEssayHistorysNum" resultType="int">
        select count(*) from essayhistory as eh,essay as e where eh.essayid=e.id
        <if test="search != null and search != ''">
           and e.title like CONCAT('%', #{search}, '%')
        </if>
    </select>
</mapper>