<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="dao.CommentsDao">
    <select id="getAllCommentsByUserIdNum" resultType="int">
        select count(*) from comments where userid=#{userid} and username like "%#{search}%";
    </select>
    <select id="getAllCommentsByUserId" resultType="domain.Comments">
        select * from comments where userid=#{userid} and msg like "%#{search}%" limit #{start},#{size} order by createtime desc
    </select>
    <select id="getCommentByCommentsId" resultType="domain.Comments">
        select * from comments where id=#{commentsid}
    </select>
    <select id="getAllCommentsByEssayIdNum" resultType="int">
        select * from comments where essayid=#{essayid} order by createtime
    </select>
    <select id="getAllCommentsByEssayId" resultType="domain.Comments">
        select * from comments where essayid=#{essayid} order by createtime limit #{start},#{size}
    </select>
    <delete id="deleteComments">
        delete * from comments where id=#{commentid}
    </delete>
    <select id="getAllCommentsNum">
        select count(*) from comments where msg like "%search%"
    </select>
    <select id="getAllComments">
        select * from comments where msg like "%search%" order by createtime limit #{start},#{size}
    </select>
</mapper>