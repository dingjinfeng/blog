<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="dao.LetterDao">
    <select id="getAllChatroomByUserIdNum" resultType="int">
        select count(*) from letter where userid1=#{userid} or userid2=#{userid};
    </select>
    <select id="getAllChatroomByUserId" resultType="domain.Letter">
        select * from letter where (userid1=#{letter.userid1} and userid2=#{letter.userid2}) or (userid2=#{letter.userid1} and userid1=#{letter.userid2})
    </select>
    <delete id="deleteChatroom">
        delete from letter where (userid1=#{letter.userid1} and userid2=#{letter.userid2}) or (userid2=#{letter.userid1} and userid1=#{letter.userid2})
    </delete>
</mapper>